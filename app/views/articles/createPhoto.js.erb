if(<%= @photo.id.to_s.html_safe %>){
	var image = new Object();
	image.type = "img";
	image.id = "<%= @photo.id.to_s.html_safe %>";
	image.path = "<%= @photo.image.to_s.html_safe %>";
	
	if($("#newImageLink").val()){
		image.link = $("#newImageLink").val();
	}

	editor.resetChild();
	editor.img.update(image);
	
}
else{
	editor.alert("已經上傳過同樣名稱的圖片", "error");
}
